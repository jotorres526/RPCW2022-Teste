extends layout

block content
  div.w3-container
  h1 Lista de Termos de Indice
  table.w3-table.w3-striped
      tr
        th Termo
        th ID Classe
        th Titulo Classe
      for t in termos
        tr
          td= t.termo
          td
            a(href='/classes/' + t.idClasse)= t.idClasse
          td
            a(href='/classes/' + t.idClasse)= t.tituloClasse
  div.w3-container.w3-light-grey
    p 
      footer
        a(class='w3-button w3-round-large w3-border' href='/') Início
      